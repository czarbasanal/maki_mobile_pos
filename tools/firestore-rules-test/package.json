{
  "name": "maki-firestore-rules-test",
  "version": "0.1.0",
  "private": true,
  "description": "Tests for firestore.rules using @firebase/rules-unit-testing against the Firebase emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-maki-pos 'mocha --timeout 20000'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^14.6.0",
    "mocha": "^11.0.1"
  }
}
